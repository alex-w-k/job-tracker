<nav>
  <div class="nav-wrapper">
    <div class="col s11">
      <%= link_to 'Home', root_path, class: "breadcrumb"%>
      <%= link_to @job.company.name, company_path(@job.company), class: "breadcrumb" %>
      <%= link_to @job.title, company_job_path(@job.company, @job), class: "breadcrumb" %>
      <a href="#!" class="breadcrumb">Edit</a>
    </div>
  </div>
</nav>

<h2>Edit <%= @job.title %> at <%= @company.name %> here!</h2>

<%= render partial: "form" %>
