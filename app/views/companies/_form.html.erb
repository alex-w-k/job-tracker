<div class="row">
  <div class="col s6">
    <%= form_for @company do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit 'Submit', class: "btn waves-effect waves-light" %>
    <% end %>
  </div>
</div>
